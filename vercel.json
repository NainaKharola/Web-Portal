{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd web-portal && npm install && npm run build",
  "outputDirectory": "web-portal/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}